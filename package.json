{
  "name": "cancellation-monitoring-system",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently --raw -n backend,frontend -c blue,magenta \"pnpm.cmd --dir backend dev\" \"pnpm.cmd --dir frontend dev\"",
    "dev:backend": "pnpm --dir backend dev",
    "dev:frontend": "pnpm --dir frontend dev",
    "install:all": "pnpm --dir backend install && pnpm --dir frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "packageManager": "pnpm@11.2.2",
  "dependencies": {
    "express-rate-limit": "^8.6.0",
    "googleapis": "^173.0.0",
    "motion": "^13.2.0"
  }
}
